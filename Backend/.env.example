# GitHub Models API Configuration
# Copy this file to .env and add your actual GitHub token

# Get your GitHub token from: https://github.com/settings/tokens
# Ensure it has access to GitHub Models (read:packages scope)
GITHUB_TOKEN=your-github-token-here

# Optional: Override default model (openai/gpt-4o-mini is default)
# GPT_MODEL=openai/gpt-4o-mini

# Optional: Set daily query limit (default: 1000)
# DAILY_QUERY_LIMIT=1000
